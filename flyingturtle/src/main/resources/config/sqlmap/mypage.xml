<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.flyingturtle.repository.mapper.MypageMapper">
	
  <!-- 전체 리스트 조회 -->
 	<select id="listMypage" parameterType="int" resultType="Member">
   	   select * 
   	     from tb_member
   	    where member_no = #{memberNo}
    </select>
    
    
  <!-- 작성글보기 -->
    <select id="listMyWrite" parameterType="int" resultType="Member">
       select title 
		from tb_member m inner join tb_video v on m.member_no = v.member_no 
	   where m.member_no = #{memberNo}
	     union
		select title
		 from tb_member m inner join tb_question q on m.member_no =q.member_no
		where m.member_no = #{memberNo}	 
    </select>
    
    
    <!-- 작성댓글 보기 -->
     <select id="listMyComment" parameterType="int" resultType="Member">
      select content
     from tb_qna_com q inner join tb_member m on q.member_no = m.member_no
     where m.member_no=#{memberNo}	
	union
     select content
     from tb_video_com v inner join tb_member m on v.member_no = m.member_no
     where m.member_no=#{memberNo}	
     </select>
     
       
    <!-- 비디오 작성글 개수 보기   -->
     <select id="countWriteVideo" parameterType="int" resultType="int">
       SELECT count(video_no)
   		 FROM tb_video
    	where member_no=#{memberNo} 
     </select>
       
    <!-- 큐엔에이 작성글 개수 보기   -->
     <select id="countWriteQna" parameterType="int" resultType="int">
       SELECT count(qna_no)
   		 FROM tb_question
    	where member_no=#{memberNo}
     </select>
        
     <!-- 비디오 댓글 개수 보기 --> 
      <select id="countCommentVideo" parameterType="int" resultType="int">   
        SELECT count(com_no)
   		 FROM tb_video_com
    	where member_no=#{memberNo}  
	  </select>
	  
	  <!-- 큐엔에이 댓글 개수 보기 --> 
	  <select id="countCommentQna" parameterType="int" resultType="int">   
 	    select count(com_no) 
         from tb_qna_com
         where member_no=#{memberNo} 
     </select>
     
     
 </mapper>